# gb-526 商业潜水减压暴露控制

请生成英文短名为 `commercial-diving-decompression-control` 的 Go 全栈 Web 项目。系统用于商业潜水培训与作业前的离线暴露建模，记录计划深度段、呼吸气体假设和组织负荷估算，帮助复核人员比较方案。它不是医疗器械、认证潜水表或现场生命支持系统，不得连接潜水设备、不得生成可直接执行的减压指令、不得代替潜水主管和医疗专业人员；所有结果必须标为训练/决策支持并人工审批。项目不含排班预约、工单、库存、采购、订单、保险或财务功能。

## 项目主要需求

### 1. 核心实体

以下 4 个实体均需贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router 和前端 types/api/store/page：

- `DiverProfile`：字段至少包含 `id`、`profile_code`、`display_name`、`qualification_level`、`default_o2_fraction`、`default_he_fraction`、`profile_status`、`limits_note`、`version`；只保存最小化的训练资料，不保存诊断性医疗记录。
- `DivePlan`：字段至少包含 `id`、`plan_code`、`diver_profile_id`、`worksite_pressure_bar`、`breathing_mix_json`、`plan_status`、`created_by`、`reviewed_by`、`version`、`planned_at`；状态和输入版本必须可追踪。
- `ExposureSegment`：字段至少包含 `id`、`plan_id`、`sequence_no`、`depth_m`、`duration_min`、`ascent_rate_mmin`、`gas_mix_json`、`segment_type`、`notes`；序号唯一，深度、时间和气体比例必须严格校验。
- `DecompressionAssessment`：字段至少包含 `id`、`plan_id`、`assessment_status`、`algorithm_version`、`input_snapshot_json`、`compartment_loads_json`、`risk_flags_json`、`comparative_score`、`assumptions_json`、`created_at`、`reviewed_at`；结果不可覆盖，必须可重放。

### 2. 核心页面

- `/divers` 训练档案页：消费 `DiverProfile`、`DivePlan`，维护最小资料并查看关联方案。
- `/plans` 计划编排页：消费 `DivePlan`、`ExposureSegment`，编辑有序暴露段并校验连续性。
- `/exposures` 暴露剖面页：消费 `ExposureSegment`、`DecompressionAssessment`，以深度-时间图展示输入与组织负荷变化。
- `/assessments` 评估复核页：消费 `DivePlan`、`DecompressionAssessment`，比较方案、解释风险标记并提交人工复核。
- `/audit` 审计页：消费 `DivePlan`、`DecompressionAssessment`，查看输入版本、状态流转和复核动作。

### 3. 差异算法与状态流

- 实现仅供训练比较的确定性多组织舱惰性气体负荷模型：按暴露段计算环境压力、吸入惰性气体分压和每个配置化半时舱的负荷变化；固定输入必须输出固定结果。
- 校验气体组分和为 1、深度/时间非负、序号连续、上升速率边界及段间深度连续；输入不合法时不得计算。
- 输出组织负荷曲线、相对变化、假设和风险标记，只能用于比较方案。不得输出“安全可潜”“可执行停留深度/时长”等操作性结论，页面和 API 均附安全免责声明。
- `DivePlan` 状态固定为 `draft -> modeled -> pending_supervisor_review -> approved_for_training -> archived`；评估失败回到 draft，批准必须由 supervisor 角色执行，状态迁移使用事务、乐观锁并记录理由。

### 4. 横切关注点

- JWT/RBAC 角色至少 `planner`、`supervisor`、`admin`，联动角色数据、后端中间件/路由、前端守卫、按钮显隐和类型。
- 审计覆盖档案变更、计划段、模型参数、运行结果、状态流转与复核，记录 request ID、操作者和前后摘要且不可普通删除。
- 全局错误处理统一气体比例错误、序列冲突、模型输入越界、状态冲突与无权限；前端不得把失败显示为通过。
- 独立实现请求追踪、结构化访问日志、panic recovery 和本地限流；日志不得包含 JWT 或不必要的个人资料。

### 5. 共享枚举、组件、hooks 与中间件

- `PlanStatus = draft | modeled | pending_supervisor_review | approved_for_training | archived`：后端 `backend/internal/constants/plan.go`，前端 `frontend/src/types/plan.ts`。
- `RiskBand = informational | caution | elevated | invalid`：后端 `backend/internal/constants/risk.go`，前端 `frontend/src/types/risk.ts`。
- README 列出枚举在数据库约束、后端各层和前端 store/component/page 的全部位置。
- 共享组件至少 `SafetyBoundaryBanner`（所有业务页）、`PlanStatusBadge`（计划/评估页）、`AssumptionPanel`（暴露/评估页）。
- hooks 至少 `useAuth`、`useAssessmentPolling`，不得复制到页面。
- 中间件至少独立实现 `RequestIDMiddleware`、`AccessLogMiddleware`、`AuthMiddleware`、`RBACMiddleware`、`RecoveryMiddleware`、`RateLimitMiddleware`。

### 6. 技术与规模红线

- React 18 + TypeScript + Vite + Material UI + Zustand + ECharts，图表消费真实 API 数据并有空态/错误态。
- Go 1.22 + Gin + GORM + PostgreSQL，validator/v10、结构化日志、构造器注入、事务、`%w` 错误包裹和优雅停机。
- 功能 Go 代码目标 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；不计测试、vendor、生成代码和前端。禁止空壳文件、复制样板和单文件大杂烩。
- API 统一 `/api/v1`，真实 `/healthz`、`/readyz`、统一响应/分页/错误码；气体校验、组织舱计算、快照与状态机必须有表驱动测试。
- 禁止 TODO、placeholder、随机结果、handler 直接访问数据库、伪造医疗或安全认证结论。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,decompression,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

四实体逐层分文件；压力换算、气体校验、组织舱计算、风险标记分文件。严禁合并职责到单一文件。

根目录 `runtime_smoke.json` 只能是启动 manifest，禁止填入构建、API、Browser 或截图结果。必须提交以下可解析内容；后端在 smoke 环境使用 GORM SQLite 独立内存库，在 Compose 环境默认使用 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20526",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb526-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20526/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 项目名 `commercial-diving-decompression-control`；前端 `18526`、后端 `19526`、PostgreSQL `57526`，容器内 `80/8080/5432`。
- Compose 顶层 `name` 且无 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-commercial-diving-decompression-control}` 前缀；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例包含 Compose 名、端口、数据库变量、JWT、CORS、模型版本、最大段数和日志级别。Nginx `/api` 反代 `backend:8080` 并支持 SPA；中文目录一键启动。
- README 中文且 Docker 启动优先，包含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、模型公式/假设、醒目安全边界、开发命令、排错和 License。

### 9. 强制真实验收

1. 根目录真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端执行依赖安装、类型检查和 `npm run build`。
2. 执行 Compose config、构建启动、ps 与健康/就绪检查，确认数据库/后端 healthy 和前端可访问。
3. 至少 8 项接口冒烟：登录、档案创建、计划创建、暴露段创建/排序、非法气体拒绝、模型运行、结果查询、提交复核、主管批准、越级拒绝和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 仅用 Codex 内置 Browser 验证登录、5 页面、暴露图、模型假设、安全提示、复核和权限显隐；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 验收后执行 `docker compose down -v --remove-orphans` 并确认本项目容器为空，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
